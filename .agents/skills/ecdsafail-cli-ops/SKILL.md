---
name: ecdsafail-cli-ops
description: "Storm repo-local skill for safe ecdsa.fail CLI benchmark, validation, notes, sync, reset, and submit operations."
license: MIT
---

# ECDSA.fail CLI Ops

This is a Codex-discoverable bridge to the Storm prompt card at
`../../../skills/ecdsafail-cli-ops.md`.

Before running `ecdsafail` commands that affect benchmark state, read that card
and follow its command context, output, and kill gate. If the global
`ecdsafail-cli` skill is available, load it first for detailed CLI syntax.

This bridge is local-only. It does not load private chat logs, private
endpoints, raw logs, nonces, telemetry, or always-on behavior.
